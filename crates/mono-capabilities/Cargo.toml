[package]
name = "mono-capabilities"
version = "0.1.0"
edition = "2024"

[dependencies]
mono-core = { path = "../mono-core/" }
serde = { version = "1", features = ["derive"] }
toml = "1.1.2+spec-1.1.0"
anyhow = "1"
tokio = { version = "1", features = ["full"] }